import React from 'react';

export default function Section4({ isActive }) {
    return (
        <section className="section w-full h-full bg-white flex flex-col items-center justify-center relative px-4">
            {/* 빈 캔버스 (추후 지시사항에 따라 콘텐츠 추가 예정) */}
        </section>
    );
}
